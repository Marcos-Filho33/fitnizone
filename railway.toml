[build]
builder = "nixpacks"
buildCommand = "cd backend && npm run build"
startCommand = "cd backend && node dist/server.js"
# Run install inside the backend folder to avoid workspace flags in CI
installCommand = "cd backend && npm install --no-package-lock"
